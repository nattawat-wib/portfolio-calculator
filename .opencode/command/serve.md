---
description: Start a local HTTP server to preview the mini-apps
---

Start a local development server in the project root directory. Use Python's built-in HTTP server on port 8080 so the user can preview the mini-apps in a browser at http://localhost:8080.

Run: `python3 -m http.server 8080`
