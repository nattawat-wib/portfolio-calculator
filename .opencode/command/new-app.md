---
description: Scaffold a new mini-app using the new-app agent
agent: new-app
---

Create a new mini-app inside this project. The user's request is: $ARGUMENTS

Follow the conventions documented in your system prompt. Create all necessary files. After creating the app, tell the user to run `add-to-portal` command to register it in the portal.
